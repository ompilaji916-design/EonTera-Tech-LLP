import { pageMetadata } from "@/lib/seo";
import { PageHero, ContactBand, SectionHead } from "@/components/ui";
import { NEWS_RECORDS } from "@/lib/data/platform";
const categories = ["Company news","Product launches","Announcements","Events / news","Press coverage","Publications","Media mentions","Videos"];
export const metadata = pageMetadata("News & Media", "EonTera company news, product launches, announcements, events, press coverage, publications, media mentions and videos.", "/news-media/");
export default function Page(){ return <><PageHero eyebrow="NEWSROOM" title={<>News, media and<br/><em>company updates.</em></>} text="A CMS-ready newsroom organised for company announcements, launches, event coverage, publications, press and video."/><section className="section"><div className="container"><div className="section-topline"><SectionHead eyebrow="MEDIA CENTRE" title={<>Published with<br/><em>source discipline.</em></>}/><p className="side-intro">Only approved EonTera material is published here.</p></div><div className="filter-pills">{categories.map((x)=><span className="static-pill" key={x}>{x}</span>)}</div>{NEWS_RECORDS.length ? <div className="news-grid">{NEWS_RECORDS.map((n)=><article key={n.slug}><p className="eyebrow">{n.type}</p><h3>{n.title}</h3><p>{n.excerpt}</p></article>)}</div>:<div className="empty-state editorial-empty"><h2>No verified newsroom items are published yet.</h2><p>The content model is ready for dated articles, media links, launch notes and video records once EonTera approves them.</p></div>}</div></section><ContactBand/></> }
