import { pageMetadata } from "@/lib/seo";
import { PageHero, ContactBand, SectionHead } from "@/components/ui";
import { EVENT_RECORDS } from "@/lib/data/platform";
export const metadata = pageMetadata("Events", "EonTera exhibitions, conferences, trade shows, upcoming events, past events and event gallery.", "/events/");
export default function Page(){ const upcoming=EVENT_RECORDS.filter(x=>x.status==="upcoming"), past=EVENT_RECORDS.filter(x=>x.status==="past"); return <><PageHero eyebrow="EVENTS" title={<>Meet. Learn.<br/><em>Build connections.</em></>} text="The event system is ready for exhibitions, conferences, trade shows, past-event records and approved galleries."/><section className="section"><div className="container"><SectionHead eyebrow="UPCOMING EVENTS" title={<>Where EonTera is<br/><em>showing up next.</em></>}/>{upcoming.length?<div className="event-grid">{upcoming.map(e=><article key={e.slug}><h3>{e.title}</h3><p>{e.location}</p></article>)}</div>:<div className="empty-state"><h2>No verified upcoming events are published.</h2><p>Dates and venues will appear only after confirmation.</p></div>}</div></section><section className="section collection-section"><div className="container"><SectionHead eyebrow="PAST EVENTS & GALLERY" title={<>A record of<br/><em>participation.</em></>}/>{past.length?<div className="event-grid">{past.map(e=><article key={e.slug}><h3>{e.title}</h3><p>{e.summary}</p></article>)}</div>:<div className="empty-state"><h2>No approved past-event archive yet.</h2><p>Verified event coverage and galleries can be added through the event data collection.</p></div>}</div></section><ContactBand/></> }
